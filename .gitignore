node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local

# Распакованный вход — в репозитории лежат исходные архивы в InBox/
InBox/_unpack/
InBox/_doc/

# Примерка фотографий: чужие товарные снимки, скачанные локально, чтобы
# посмотреть вёрстку карточки с реальными фото. В репозиторий и на сайт они
# не попадают — публиковать их нельзя. Боевые снимки придут из официальной
# медиабиблиотеки бренда (§12 п.5 хендоффа).
public/preview-photos/
content/photos.local.json

# Кэш Python от скриптов прогона редакторов
__pycache__/
